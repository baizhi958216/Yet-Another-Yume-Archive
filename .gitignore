node_modules/
dist/
target/
*.local
.DS_Store

# generated provider bundle config (never commit provider names into host history)
bundle.config.json
bundle.config.json.*

# android release signing (local only, never commit)
src-tauri/gen/android/keystore.properties

# tauri mobile generated projects keep their own ignores
src-tauri/gen/android/.gradle
src-tauri/gen/android/build
src-tauri/gen/android/app/build
